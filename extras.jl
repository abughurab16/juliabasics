#using Pkg # package manager

# Pkg.update() to update pkg completely

# Pkg.add("package name") to add a package

# using Package // this imports all functions of the package
# so you can just use the functions directly

# import Package // this allows you to specify which function
# the package is calling from

# using Plots

# plot!(1:15, x -> sin(x) * 2)

# using DataFrames

# df = DataFrame(A = [1,2], B = [pi, 23.4])
# df[row, column]
# println(df)